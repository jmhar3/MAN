import * as React from "react";
import { Box, Heading } from "@chakra-ui/react";

export const Packages = () => {
  return (
    <Box h="100vh" id="packages" alignContent="center">
      <Heading>Packages + Rates</Heading>
    </Box>
  );
};
