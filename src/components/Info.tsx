import * as React from "react";
import { Box } from "@chakra-ui/react";
import { SectionContainer } from "./SectionContainer";

export const Info = () => {
  return <SectionContainer label="Working with me" colorScheme="light"/>;
};
