import * as React from "react";
import { Box, Text } from "@chakra-ui/react";

export const Packages = () => {
  return (
    <Box h="100vh" id="packages">
      <Text>Packages + Rates</Text>
    </Box>
  );
};
